// write a function that will takes in userName, welcome message and print out concante and return it for you


function myf(userName, welcomeMessage){
    const result = `$(userName) $(welcomeMessage)` ;
return result;
}
                // Function Call
    console.log(
        
            (prompt("Enter your username")),
            (prompt("Enter your welcomemessage")),
            
        
    )

    // console.log(`My username is ${userName}  and welcome ${welcomeMessage}`);